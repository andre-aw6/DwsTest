const initialState = {
    list: [
        {title: 'Primeira Banda', body:'Testando 1, 2, 3...'}
    ]
}


export default (state = initialState, action) => {

    switch(action.type) {
        case 'ADD_BAND':

        break;
    }
    
    return state;
}